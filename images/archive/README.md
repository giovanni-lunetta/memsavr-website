# Image Archive

This folder keeps legacy website images that are no longer referenced by the live site.

- `legacy_app_screenshots/`: old versioned app screenshots removed from active website usage on 2026-03-18.

Active site images should remain in `../`.
